<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mental Health Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-purple-50 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-center mb-8">Welcome, <%= user.firstName %>!</h1>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div class="bg-white rounded-xl shadow-sm p-6">
                <h3 class="text-lg font-semibold mb-2">Current Streak</h3>
                <p class="text-2xl font-bold text-orange-600"><%= streakDays %> days</p>
            </div>
            <div class="bg-white rounded-xl shadow-sm p-6">
                <h3 class="text-lg font-semibold mb-2">Average Mood</h3>
                <p class="text-2xl font-bold text-blue-600"><%= avgMood %>/10</p>
            </div>
            <div class="bg-white rounded-xl shadow-sm p-6">
                <h3 class="text-lg font-semibold mb-2">Achievements</h3>
                <p class="text-2xl font-bold text-green-600"><%= achievements.length %></p>
            </div>
        </div>

        <!-- Dynamic content from server -->
        <div class="bg-white rounded-xl shadow-sm p-6">
            <h2 class="text-xl font-semibold mb-4">Recent Mood Entries</h2>
            <% if (moodEntries.length > 0) { %>
                <div class="space-y-3">
                    <% moodEntries.slice(0, 5).forEach(entry => { %>
                        <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                            <span>Mood: <%= entry.mood_score %>/10</span>
                            <span class="text-sm text-gray-500">
                                <%= new Date(entry.created_at).toLocaleDateString() %>
                            </span>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p class="text-gray-500">No mood entries yet. <a href="/mood" class="text-blue-600 hover:underline">Start tracking!</a></p>
            <% } %>
        </div>
    </div>
</body>
</html>
